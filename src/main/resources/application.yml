server:
  port: 8088

spring:
  application:
    name: user-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring-security?useUnicode=true&characterEncoding=utf8&characterSetResults=utf8&serverTimezone=UTC&useSSL=false
    username: root
    password: gxfc2012
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
